rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /images/{allPaths=**} {
      // Allow read for everyone (images are public), restrict write for now
      allow read: if true;
      allow write: if true; // TEMP: tighten this before production
    }
  }
}
